{
  "schema": "v3",
  "id": "concurrentworkers",
  "title": "formbio/nf-testworkflow",
  "workflowVersion": "v0.1.0",
  "displayName": "Concurrent Workers",
  "description": "This workflow is a used to verify that the concurrent workflow workers can run",
  "type": "object",
  "categories": [
    "Test"
  ],

  "inputs": {
    "concurrentworkers": {
      "title": "Concurrent Workers",
      "description": "Number of concurrent test workers to run",
      "hidden": false,
      "default": "100",
      "required": true,
      "type": "string"
    }
  },

  "ui": {
    "inputs": [
        {
            "title": "Workflow Inputs",
            "description": "Workflow Inputs",
            "hidden": false,
            "help_text": "inputs to run workflow",
            "id": "workflowinputs",
            "fields": [
                "concurrentworkers"
            ]
        }
      ]
  }
}